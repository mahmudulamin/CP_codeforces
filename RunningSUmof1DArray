class Solution {
public:
    vector<int> runningSum(vector<int>& nums) {
        int ini_sum = 0;
        int n = nums.size();
        vector<int>v;
        for(int i=0;i<n;i++)
        {
            ini_sum += nums[i];
            v.push_back(ini_sum); 
        }
        return v;
    }
};
