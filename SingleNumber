class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int n = size(nums);
        int ans = 0;
        for(int i=0;i<n;i++)
        {
            ans^=nums[i];
        }
        return ans;
    }
};
//Facebook er interview te ashcilo.
