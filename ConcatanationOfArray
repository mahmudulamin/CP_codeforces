class Solution {
public:
    vector<int> getConcatenation(vector<int>& nums) {
        int n = nums.size();
        for(int i = 0;i<n;i++)
        {
            nums.push_back(nums[i]);
        }
        return nums;
    }
};
//ei problem tay 1 ta array 2 bar print korte bola hoice.
//eijnno ami first a vector er  size ta ekta variable er modde rakhci..
//then push_back kore disi vector er sesh a
